# Producción (VPS, mismo origen detrás de nginx).
# Vacío => el frontend usa rutas relativas (/api) y el mismo origen para socket.io.
# Así funciona igual por IP (http://2.25.68.62) y por dominio (https://ecoit.site) sin recompilar.
VITE_BACKEND_URL=
VITE_SOCKET_URL=

# Cloudinary (subida de imágenes desde el navegador)
VITE_CLOUDINARY_CLOUD_NAME=dwx3v7vex
# FALTA: necesario para subir imágenes desde el navegador (pídelo en tu panel de Cloudinary > Upload presets)
VITE_CLOUDINARY_UPLOAD_PRESET=
